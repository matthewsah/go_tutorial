packages live at the directory level
typically when you createa  new file it will have package <lastDirectoryInFilePath>, though it is not required
files live at the directory level, can access other files from the same directory without any imports